#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <string>
#include <list>
#include <queue>
#include <stack>
#include <vector>
#include <algorithm>
#include <iterator>
#include <map>
#include <set>

typedef long long ll;

using namespace std;

#define maxn 10000
int c[maxn];
int num[maxn];
int N;

int main(){
	freopen("in.txt", "r", stdin);
	scanf("%d", &N);
	memset(ans, 0, sizeof(num));
	for(int i = 1; i < n; i++)
		scanf("%d", &num[i]);
	return 0;
}
